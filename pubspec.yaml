name: photo_gallery
description: Flutter Project Developed for Both IOS and Android
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  bloc: ^8.0.3
  cached_network_image: ^3.2.1
  cloud_firestore: ^3.1.16
  cupertino_icons: ^1.0.4
  dartz: ^0.10.1
  firebase_auth: ^3.3.18
  firebase_core: ^1.17.0
  firebase_storage: ^10.2.16
  flutter:
    sdk: flutter
  #flutter Bloc for architecture of the application
  flutter_bloc: ^8.0.1
  formz: ^0.4.1
  freezed_annotation: ^2.0.3
  google_fonts: ^3.0.1
  photo_view: ^0.13.0
  image_picker: ^0.8.5+3
  json_annotation: ^4.5.0
  shimmer: ^2.0.0

  # dependency injection
  injectable: ^1.5.3
  get_it: ^7.2.0

  #image compressor
  flutter_native_image: ^0.0.6+1
  flutter_mobx: ^2.0.6
  mobx: ^2.0.7+2
  pull_to_refresh: ^2.0.0

  # color for flex
  flex_color_scheme: ^5.0.1

  # icon collections from font awesome
  font_awesome_flutter: ^10.1.0
  internet_connection_checker: ^0.0.1+4
  shared_preferences: ^2.0.15
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  flutter_gen: ^4.1.6
  injectable_generator: ^1.5.3
  build_runner: ^2.1.11 # flutter pub run build_runner build --delete-conflicting-outputs
  freezed: ^2.0.3+1
  json_serializable: ^6.2.0
  mobx_codegen:


# The following section is specific to Flutter.
flutter:
  uses-material-design: true
  # generate: true

  assets:
    - assets/