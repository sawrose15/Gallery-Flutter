void main() {
  // group('app', () {
  //   testWidgets('render Appview', (tester) async {
  //     await tester.pumpWidget(const App());
  //     expect(find.byType(AppView), findsOneWidget);
  //   });
  // });

  // group('AppView', () {
  //   testWidgets('renderHomePage', (tester) async {
  //     await tester.pumpWidget(const AppView());
  //     expect(find.byType(const HomePage()), findsOneWidget);
  //   });
  // });
}
