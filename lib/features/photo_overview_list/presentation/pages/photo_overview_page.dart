import 'package:flutter/material.dart';

class PhotoOverviewPage extends StatelessWidget {
  const PhotoOverviewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
